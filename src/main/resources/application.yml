spring:
  application:
    name: pos-system

  datasource:
    url: jdbc:mysql://${DB_HOST:your-aiven-host}:${DB_PORT:28373}/${DB_NAME:pos}?useSSL=true&trustServerCertificate=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: ${DB_USERNAME:your-username}
    password: ${DB_PASSWORD:your-password}

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: your email
    password: your gmail app password
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  devtools:
    livereload:
      enabled: true

server:
  port: 5000

razorpay:
  api:
    key: your razorpay key
    secret: provide yor razorpay secrate

stripe:
  api:
    key: provide your stripe api key

gemini:
  api:
    key: provide your gemin api key optional

app:
  frontend:
    reset-url: http://localhost:5173/auth/reset-password?token=

logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
        type:
          descriptor:
            sql: TRACE
